name: {{projectName}}
description: Main site for {{projectName}}
foundation: default

# Homepage - specifies which page folder becomes the root route (/)
index: home

theme:
  primary: '#3b82f6'

build:
  prerender: true
